## Agent skills

### Issue tracker

Issues tracked as GitHub Issues in `anticipated-chemistry-of-life/feature-probabilities`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five canonical triage labels (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (`CONTEXT.md` + `docs/adr/` at repo root, created lazily by `/domain-modeling`). See `docs/agents/domain.md`.
